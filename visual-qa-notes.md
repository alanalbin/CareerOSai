# Visual QA notes

- Desktop screenshots checked for `/`, `/student`, `/college`, and `/recruiter` at 1280x720. The landing page has a restrained navy/green/gold palette, clear hierarchy, and a subtle product preview. Role workspaces use a consistent sidebar shell, readable metrics, and distinct content for each role.
- Mobile screenshots checked for `/`, `/student`, `/college`, and `/recruiter` at 375x812. The public header collapses to a menu button, dashboard sidebars collapse behind a menu button, and metric cards stack vertically without horizontal overflow.
- Demo mode is explicitly labeled across workspaces; AI/integration states are described as setup states rather than claimed as active.
- No screenshot-only layout defects were observed. Full-page sections below the fold remain available via normal scroll.
